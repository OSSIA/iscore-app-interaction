*Installation avec : OSSIA/i-score/wiki/Build...

**1.md : ct insérer le plugin dans qt
**2.md : concepts généraux -> ex : il y a un id pour chaque objet, chemin, ..., et organisation des objets
**3.md : exemple

*Dans iscore_addon_tutorial :
** AppliPlugin : éléments qui sont tjs là lorsqu'on utilise le logiciel, comme des menus
** DocPlugin : éléments propres aux différentes pages du logiciel qu'on ouvre
**Process/ProcessModel : exemple d'implémentation d'un processus
/!\ au undo/redo : on ne peut pas juste "set" une variable, il faut pouvoir annuler les commandes exécutées. --> utilisation d'un pattern commant ; on implémente un objet avec undo et redo

** Panneau de droite avec les infos sur l'objet = Inspector
**PolymorphicEntity : ça n'est pas pour nous.

* Qt :
F2 envoie à la classe de base de l'élement sur lequel est placé le curseur. Utile pour voir la doc de l'objet
F5 (ou F6) pour passer de .cpp au .hpp

(retour addon : )
**Layer : partie graphique (affichage des boites)
--> Presenter : lien entre la vue et le modèle -> met à jour la vue, récupère les interaction utilisateur
--> vue : dessins
** Exec : execution (lecture)
** LocalTree : on s'en fout.
** ProcessModel : classe de base contenant les données
** Commands : /!\ undo/redo
** Panel : ajouter des panneaux (pas très utile pour nous)
** Setting : preferences utilisateur

** Process : le + important pour nous.
 -----------

Dans Qt : changer gcc et g++ en gcc6 et g++6 , changer la version de qt, changer la version de cmake, (les changer dans kits/desktops aussi)

